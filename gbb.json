{
    "version": "0.5.0",
    "tool": "go install -ldflags='-s -w'",
    "importpath": "github.com/voidint/tsdump/build",
    "variables": [
        {
            "variable": "Date",
            "value": "{{.Date}}"
        },
        {
            "variable": "Commit",
            "value": "{{.GitCommit}}"
        }
    ]
}